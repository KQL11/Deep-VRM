### Modifications

This version includes the following changes:

1. Uses the original visual encoder to extract low-level features through full fine-tuning or LoRA fine-tuning, and names it the Low-Level Visual Encoder.
2. Adds the features from the Low-Level Visual Encoder directly to the original Vision Encoder.


